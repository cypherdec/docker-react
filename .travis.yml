language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t sanduljayathileka/docker-react -f Dockerfile.dev .  

script:
  - docker run -e CI=true sanduljayathileka/docker-react npm run test  